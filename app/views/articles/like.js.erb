jQuery("#thumbs-up-wrapper-<%= @article.id %>").html('<%= j render "like_icon", {article: @article, like: true} %>');
jQuery("#thumbs-down-wrapper-<%= @article.id %>").html('<%= j render "like_icon", {article: @article, like: false} %>');

 jQuery(".article-thumbs-up, .article-thumbs-down").unbind().click(function(event) {
      jQuery.ajax({
          url: this.dataset.url,
          method: 'POST',
          success: function(result) {
          }
      })
  })